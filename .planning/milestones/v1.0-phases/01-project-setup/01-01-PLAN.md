---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - tailwind.config.ts
  - postcss.config.mjs
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Next.js dev server starts without errors on localhost:3000"
    - "TypeScript compiles without errors"
    - "Default home page renders in the browser"
    - "Firebase SDK is installed as a dependency"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with Next.js and Firebase dependencies"
      contains: "firebase"
    - path: "src/app/layout.tsx"
      provides: "Root layout wrapping all routes"
      contains: "RootLayout"
    - path: "src/app/page.tsx"
      provides: "Home page component"
      contains: "export default"
    - path: "tsconfig.json"
      provides: "TypeScript configuration with path aliases"
      contains: "@/*"
    - path: "next.config.ts"
      provides: "Next.js configuration"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "src/app/layout.tsx"
      to: "src/app/page.tsx"
      via: "children prop rendering"
      pattern: "children"
---

<objective>
Scaffold the Next.js project with TypeScript, Tailwind CSS, ESLint, and App Router, then install the Firebase SDK as a dependency.

Purpose: Creates the project foundation that all subsequent phases build on. Without a working Next.js project with Firebase installed, nothing else can proceed.
Output: A running Next.js 16 project at localhost:3000 with Firebase SDK available for import.
</objective>

<execution_context>
@/Users/francisdall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/francisdall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and install Firebase</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    eslint.config.mjs
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    tailwind.config.ts
    postcss.config.mjs
    .gitignore
  </files>
  <action>
    Run create-next-app from the project root to scaffold the Next.js project. The project files should be created IN the current directory (beer_glass_app), not in a subdirectory.

    Since the directory already has a .git and .planning folder, use this approach:
    1. Create a temporary directory and scaffold there:
       ```
       npx create-next-app@latest temp-scaffold --typescript --eslint --tailwind --app --turbopack --src-dir --import-alias "@/*" --yes
       ```
    2. Move all scaffolded files (except .git) from temp-scaffold/ into the project root
    3. Remove the temp-scaffold directory
    4. Ensure .gitignore includes node_modules, .next, .env.local, and .env*.local

    After scaffolding, install the Firebase SDK:
    ```
    npm install firebase
    ```

    Then clean up the default page.tsx to show a minimal home page instead of the Next.js boilerplate. Replace the contents of src/app/page.tsx with a simple component that renders:
    - An h1 heading: "Beer Glass App"
    - A paragraph: "Find the right glass for your beer."

    Keep it as a Server Component (no 'use client' directive). Use basic Tailwind classes for centered layout (flex, items-center, justify-center, min-h-screen, flex-col, gap-4).

    Do NOT modify layout.tsx beyond what create-next-app generates. The default root layout with Inter/Geist font and globals.css import is fine.
  </action>
  <verify>
    1. Run `npm run build` -- should complete without errors
    2. Run `npm run dev` and verify localhost:3000 shows "Beer Glass App" heading
    3. Confirm `firebase` appears in package.json dependencies: `grep firebase package.json`
    4. Confirm TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
    Next.js project builds and serves at localhost:3000 showing "Beer Glass App" heading. Firebase SDK is in package.json dependencies. TypeScript compiles without errors. ESLint and Tailwind are configured.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with exit code 0
2. `npm run dev` starts dev server at localhost:3000
3. Home page renders with "Beer Glass App" heading
4. `grep "firebase" package.json` shows firebase in dependencies
5. `npx tsc --noEmit` exits cleanly
</verification>

<success_criteria>
- Next.js 16+ project exists in project root with App Router structure
- TypeScript, ESLint, Tailwind CSS all configured and working
- Firebase SDK installed (importable but not yet initialized)
- Dev server runs without errors
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>
