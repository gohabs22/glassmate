---
phase: 02-role-selection-authentication
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/components/auth/SignupForm.tsx
  - src/components/auth/LoginForm.tsx
  - src/components/dashboard/SetupWizard.tsx
autonomous: false

must_haves:
  truths:
    - "Host can fill out signup form with email and password, submit, and account is created in Firebase"
    - "Host can fill out login form with email and password, submit, and is redirected to dashboard"
    - "Invalid credentials show specific error messages (not generic crashes)"
    - "Logged-in user visiting / is redirected to /dashboard"
    - "Anonymous visitor visiting / is redirected to /scan"
    - "Host sees setup wizard after first login prompting them to add glasses"
    - "Auth state persists across browser sessions (closing and reopening browser keeps user logged in)"
    - "Logout button on dashboard signs user out and redirects to /login"
  artifacts:
    - path: "src/components/auth/SignupForm.tsx"
      provides: "Signup form with email/password inputs, validation errors, submit handler"
      contains: "useActionState"
    - path: "src/components/auth/LoginForm.tsx"
      provides: "Login form with email/password inputs, error display, submit handler"
      contains: "useActionState"
    - path: "src/components/dashboard/SetupWizard.tsx"
      provides: "Post-signup onboarding wizard with welcome message and CTA to add glasses"
      contains: "wizard"
    - path: "src/app/page.tsx"
      provides: "Conditional redirect based on auth state"
      contains: "redirect"
    - path: "src/app/layout.tsx"
      provides: "Root layout wrapping children with AuthProvider"
      contains: "AuthProvider"
  key_links:
    - from: "src/components/auth/SignupForm.tsx"
      to: "src/lib/firebase/auth-actions.ts"
      via: "useActionState calling signup server action"
      pattern: "useActionState\\(signup"
    - from: "src/components/auth/LoginForm.tsx"
      to: "src/lib/firebase/auth-actions.ts"
      via: "useActionState calling login server action"
      pattern: "useActionState\\(login"
    - from: "src/components/auth/SignupForm.tsx"
      to: "src/lib/firebase/auth.ts"
      via: "onAuthStateChanged for redirect after signup"
      pattern: "onAuthStateChanged"
    - from: "src/app/layout.tsx"
      to: "src/components/auth/AuthProvider.tsx"
      via: "wraps children with AuthProvider"
      pattern: "<AuthProvider>"
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "src/components/dashboard/SetupWizard.tsx"
      via: "renders SetupWizard for new users"
      pattern: "<SetupWizard"
    - from: "src/components/auth/SignupForm.tsx"
      to: "cookie __session"
      via: "sets __session cookie after successful Firebase auth"
      pattern: "document\\.cookie.*__session"
---

<objective>
Wire auth forms into pages, add AuthProvider to root layout, implement conditional home page routing, create the onboarding setup wizard, and connect logout. This completes the full auth flow end-to-end.

Purpose: This plan takes the auth infrastructure (Plan 01) and page structure (Plan 02) and connects them into a working authentication system. After this plan, users can sign up, log in, see their dashboard, and log out.
Output: Complete working auth flow -- signup, login, dashboard with wizard, logout, and conditional home page redirect.
</objective>

<execution_context>
@/Users/francisdall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/francisdall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-role-selection-authentication/02-RESEARCH.md

@.planning/phases/02-role-selection-authentication/02-01-SUMMARY.md
@.planning/phases/02-role-selection-authentication/02-02-SUMMARY.md

@src/lib/firebase/auth.ts
@src/lib/firebase/auth-actions.ts
@src/lib/validations/auth.ts
@src/components/auth/AuthProvider.tsx
@src/app/layout.tsx
@src/app/page.tsx
@src/app/(auth)/login/page.tsx
@src/app/(auth)/signup/page.tsx
@src/app/(dashboard)/dashboard/page.tsx
@src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth forms, wire into pages, add AuthProvider to root layout</name>
  <files>
    src/components/auth/SignupForm.tsx
    src/components/auth/LoginForm.tsx
    src/app/(auth)/signup/page.tsx
    src/app/(auth)/login/page.tsx
    src/app/layout.tsx
  </files>
  <action>
    1. Create `src/components/auth/SignupForm.tsx` with 'use client':
       - Import useActionState from 'react', useRouter from 'next/navigation', useEffect from 'react'
       - Import onAuthStateChanged from 'firebase/auth' and auth from '@/lib/firebase/auth'
       - Import signup from '@/lib/firebase/auth-actions'
       - Use `useActionState(signup, { errors: {} })` to get [state, formAction, isPending]
       - useEffect with onAuthStateChanged observer:
         - When user becomes non-null: set a `__session` cookie (`document.cookie = '__session=1; path=/; max-age=2592000'` -- 30 days) then router.push('/dashboard')
         - Return unsubscribe cleanup
       - Render a form with `action={formAction}`:
         - Email input (type="email", name="email", required)
         - Show state.errors?.email?.[0] in red text below email input
         - Password input (type="password", name="password", required)
         - Show state.errors?.password?.[0] in red text below password input
         - Show state.errors?._form?.[0] in red text above submit button
         - Submit button showing "Creating account..." when isPending, "Create Account" otherwise
       - Style with Tailwind: labeled inputs with spacing, error text in text-red-600 text-sm, button with bg-amber-600 hover:bg-amber-700 text-white rounded px-4 py-2 disabled:opacity-50

    2. Create `src/components/auth/LoginForm.tsx` with 'use client':
       - Same pattern as SignupForm but using `login` action instead of `signup`
       - useActionState(login, { errors: {} })
       - Same onAuthStateChanged + cookie + redirect pattern
       - Form with email and password inputs
       - Error display for email, password, and _form errors
       - Submit button: "Signing in..." / "Log In"
       - Same Tailwind styling

    3. Update `src/app/(auth)/signup/page.tsx`:
       - Import and render SignupForm component (replacing the placeholder div)
       - Keep the heading "Create Account" and link to /login

    4. Update `src/app/(auth)/login/page.tsx`:
       - Import and render LoginForm component (replacing the placeholder div)
       - Keep the heading "Log In" and link to /signup

    5. Update `src/app/layout.tsx`:
       - Import AuthProvider from '@/components/auth/AuthProvider'
       - Wrap {children} with `<AuthProvider>` inside the <body> tag
       - Keep existing font classes and metadata (update title to "Beer Glass App" and description to "Find the right glass for your beer")

    IMPORTANT: The __session cookie set by the forms is what the middleware (from Plan 02) checks. It's a simple presence check cookie, not a JWT. Firebase Auth handles real session persistence via IndexedDB -- the cookie is just for middleware route protection.

    IMPORTANT: Do NOT use Firebase setPersistence explicitly. Firebase Auth defaults to browserLocalPersistence (persists across browser sessions). The existing auth.ts from Phase 1 already calls getAuth() which uses the default persistence.
  </action>
  <verify>
    Run `npm run build` to confirm everything compiles. Start dev server and verify:
    1. Visit /signup -- form renders with email and password fields
    2. Visit /login -- form renders with email and password fields
    3. Submit signup with invalid email -- Zod error appears
    4. Submit signup with short password -- Zod error appears
    5. AuthProvider wraps app (check root layout source)
  </verify>
  <done>
    SignupForm and LoginForm render in their pages. Forms use useActionState with server actions. Error messages display for validation failures. AuthProvider wraps the app. __session cookie is set on successful auth, enabling middleware redirects.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create setup wizard, wire dashboard, and implement conditional home redirect</name>
  <files>
    src/components/dashboard/SetupWizard.tsx
    src/app/(dashboard)/dashboard/page.tsx
    src/app/page.tsx
  </files>
  <action>
    1. Create `src/components/dashboard/SetupWizard.tsx` with 'use client':
       - A simple onboarding card shown to new users after signup
       - Props: `{ onDismiss: () => void }`
       - Renders a card with:
         - Heading: "Welcome to Beer Glass App!"
         - Paragraph: "Let's get started by adding your glass collection. This helps your guests find the perfect glass for their beer."
         - A primary CTA button: "Add My First Glasses" linking to "/dashboard/glasses" (Phase 3 will create this page)
         - A secondary text link: "I'll do this later" that calls onDismiss
       - Style: bordered card with amber/warm accent colors, rounded corners, padding. Primary button bg-amber-600 text-white, secondary as text-amber-700 underline.
       - The wizard shows based on a `showWizard` state that's stored in localStorage key `beer_glass_wizard_dismissed`. If the key exists, wizard is hidden. onDismiss sets the key and hides the wizard.

    2. Update `src/app/(dashboard)/dashboard/page.tsx`:
       - Convert to 'use client' component (needs useAuth hook and SetupWizard state)
       - Import useAuth from '@/components/auth/AuthProvider'
       - Import SetupWizard from '@/components/dashboard/SetupWizard'
       - Import signOut from 'firebase/auth' and auth from '@/lib/firebase/auth'
       - Import useRouter from 'next/navigation'
       - Show loading spinner/text while auth is loading (`if (loading) return <div>Loading...</div>`)
       - If user is null and not loading, redirect to /login using router.push
       - Display user email in welcome message: "Welcome, {user.email}!"
       - Render SetupWizard (conditionally, based on localStorage dismiss state -- manage in page or in wizard component itself)
       - Keep the two action cards from Plan 02: "Manage My Glasses" (/dashboard/glasses) and "Check In Somewhere" (/scan)
       - Add a working logout button:
         - onClick handler: call signOut(auth), delete __session cookie (`document.cookie = '__session=; path=/; max-age=0'`), router.push('/login')
         - Style: text button in top-right or below cards, "Log Out" text, text-gray-500 hover:text-gray-700

    3. Update `src/app/page.tsx`:
       - Convert to 'use client' component
       - Import useAuth from '@/components/auth/AuthProvider'
       - Import useRouter from 'next/navigation' and useEffect
       - In useEffect, after loading is false:
         - If user exists -> router.push('/dashboard')
         - If user is null -> router.push('/scan')
       - While loading, show a simple centered loading state (the Beer Glass App name with a subtle loading indicator)
       - Remove the Firebase connection status check (no longer needed after Phase 1 verification)

    IMPORTANT: The home page redirect uses client-side auth state (from AuthProvider) rather than server-side cookie check. This is because the __session cookie is a simple flag -- only Firebase Auth knows the real auth state. The middleware provides optimistic protection, and the client-side check provides the actual redirect logic.
  </action>
  <verify>
    Run `npm run build` to confirm everything compiles. Start dev server and test the full flow:
    1. Visit / -> redirects to /scan (when not logged in)
    2. Visit /signup -> create an account with valid email/password
    3. After signup -> redirected to /dashboard, setup wizard visible
    4. Dashboard shows user email, "Manage My Glasses", "Check In Somewhere"
    5. Dismiss wizard -> refresh page -> wizard stays dismissed
    6. Click "Log Out" -> redirected to /login, /dashboard now redirects to /login
    7. Visit /login -> log in with same credentials -> redirected to /dashboard
    8. Close browser, reopen, visit / -> redirected to /dashboard (session persisted)
  </verify>
  <done>
    Home page redirects based on auth state. Dashboard shows personalized greeting, action cards, and working logout. Setup wizard appears for new users and can be dismissed. Full auth cycle works: signup -> dashboard -> logout -> login -> dashboard.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete authentication flow: signup, login, dashboard with setup wizard, logout, and conditional routing based on auth state.</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 -> should redirect to /scan (anonymous flow)
    2. Navigate to /signup -> create a new account with email and password
    3. After signup -> should land on /dashboard with welcome message and setup wizard
    4. Verify dashboard shows "Manage My Glasses" and "Check In Somewhere" cards
    5. Dismiss the setup wizard -> refresh -> wizard should stay dismissed
    6. Click "Log Out" -> should redirect to /login
    7. Try visiting /dashboard directly -> should redirect to /login (protected)
    8. Log in with the account you created -> should reach /dashboard
    9. Visit / while logged in -> should redirect to /dashboard
    10. Close browser completely, reopen, visit / -> should still redirect to /dashboard (session persists)
    11. Try signup with invalid email or weak password -> should see error messages
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. Full signup flow: /signup -> enter email/password -> account created -> redirect to /dashboard
3. Full login flow: /login -> enter credentials -> redirect to /dashboard
4. Home redirect: / -> /dashboard (logged in) or / -> /scan (anonymous)
5. Route protection: /dashboard -> /login redirect when not authenticated
6. Setup wizard appears after first login, dismissible, stays dismissed
7. Logout clears session and redirects to /login
8. Session persists across browser close/reopen
9. Validation errors display for invalid inputs
</verification>

<success_criteria>
- SignupForm and LoginForm use useActionState with Zod-validated server actions
- AuthProvider wraps entire app in root layout
- Home page conditionally redirects based on auth state
- Dashboard shows personalized welcome, action cards, setup wizard, and logout
- Middleware + cookie system protects /dashboard routes
- Session persists across browser sessions via Firebase's default browserLocalPersistence
- Error messages are specific and helpful (not generic)
</success_criteria>

<output>
After completion, create `.planning/phases/02-role-selection-authentication/02-03-SUMMARY.md`
</output>
