---
phase: 07-audit-gap-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/SetupWizard.tsx
  - src/components/beer/ResultsSheet.tsx
  - src/app/c/[userId]/page.tsx
  - src/middleware.ts
  - src/lib/firebase/auth-actions.ts
autonomous: true

must_haves:
  truths:
    - "SetupWizard 'Add My First Glasses' CTA navigates to /glasses (not /dashboard/glasses)"
    - "Save-to-history button appears in ResultsSheet even when host has empty collection (idealGlass set)"
    - "Save-to-history call in check-in page handles empty-collection case (idealGlass as recommendation)"
    - "/history route is protected by middleware isProtectedRoute check"
    - "auth-actions.ts file is deleted (dead code removed)"
  artifacts:
    - path: "src/components/dashboard/SetupWizard.tsx"
      provides: "Fixed CTA link to /glasses"
    - path: "src/components/beer/ResultsSheet.tsx"
      provides: "Save button visible in empty-collection case"
    - path: "src/app/c/[userId]/page.tsx"
      provides: "Save handler works with idealGlass (no bestAvailable)"
    - path: "src/middleware.ts"
      provides: "/history added to protected routes"
  key_links:
    - from: "src/components/dashboard/SetupWizard.tsx"
      to: "/glasses route"
      via: "href link"
      pattern: "href=\"/glasses\""
---

<objective>
Fix all gaps identified by v1.0 milestone audit: broken SetupWizard link, empty-collection history save, middleware gap, and dead code.

Purpose: Close UX-02 and HIST-01 partial requirements so the milestone can pass re-audit.
Output: 4 targeted fixes across 5 files.
</objective>

<execution_context>
@/Users/francisdall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/francisdall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1-MILESTONE-AUDIT.md
@src/components/dashboard/SetupWizard.tsx
@src/components/beer/ResultsSheet.tsx
@src/app/c/[userId]/page.tsx
@src/middleware.ts
@src/lib/firebase/auth-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SetupWizard CTA link</name>
  <files>src/components/dashboard/SetupWizard.tsx</files>
  <action>
    In SetupWizard.tsx, find the "Add My First Glasses" link that has `href="/dashboard/glasses"` and change it to `href="/glasses"`.

    This is a 1-line fix. The `/dashboard/glasses` path returns a 404 because the glass management page lives at `/glasses` (inside the `(dashboard)` route group, which doesn't affect the URL).
  </action>
  <verify>
    Grep for `/dashboard/glasses` in SetupWizard.tsx returns no results.
    Grep for `href="/glasses"` in SetupWizard.tsx returns the CTA link.
  </verify>
  <done>SetupWizard CTA correctly navigates to /glasses. UX-02 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Fix empty-collection history save</name>
  <files>src/components/beer/ResultsSheet.tsx, src/app/c/[userId]/page.tsx</files>
  <action>
    **In ResultsSheet.tsx:**
    Remove the `{!idealGlass && (` condition wrapping the save-to-history section (around line 148). The save button should be visible regardless of whether `idealGlass` is set. Keep the rest of the save section logic intact (isLoggedIn/saved checks).

    Change from:
    ```
    {!idealGlass && (
      <div className="mt-6 border-t border-gray-100 pt-4">
        ...save button / saved / nudge...
      </div>
    )}
    ```

    To:
    ```
    <div className="mt-6 border-t border-gray-100 pt-4">
      ...save button / saved / nudge...
    </div>
    ```

    **In src/app/c/[userId]/page.tsx:**
    Update the onSaveToHistory handler to handle the empty-collection case where `bestAvailable` is null but `idealGlass` is set:

    Change the guard from:
    ```
    if (!currentUser || !selectedBeer || !matchResult?.bestAvailable) return;
    ```

    To use idealGlass as fallback:
    ```
    if (!currentUser || !selectedBeer || !matchResult) return;
    const glass = matchResult.bestAvailable
      ? { glassId: matchResult.bestAvailable.glass.id, glassName: matchResult.bestAvailable.glass.name }
      : matchResult.idealGlass
        ? { glassId: matchResult.idealGlass.id, glassName: matchResult.idealGlass.name }
        : null;
    if (!glass) return;
    ```

    Then use `glass` in the `saveHistoryEntry` call instead of the inline object.
  </action>
  <verify>
    ResultsSheet.tsx no longer conditionally hides save section based on idealGlass.
    Check-in page save handler uses idealGlass as fallback when bestAvailable is null.
    `npx tsc --noEmit` passes.
  </verify>
  <done>History save works for both normal matching and empty-collection scenarios. HIST-01 fully satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add /history to middleware protected routes</name>
  <files>src/middleware.ts</files>
  <action>
    In middleware.ts, find the `isProtectedRoute` check (around line 15) and add `/history` to the protected routes.

    Add `|| pathname.startsWith('/history')` to the existing condition.
  </action>
  <verify>
    Grep for `history` in middleware.ts returns the new route check.
  </verify>
  <done>/history is protected by middleware — unauthenticated users are redirected at the edge.</done>
</task>

<task type="auto">
  <name>Task 4: Delete dead auth-actions.ts</name>
  <files>src/lib/firebase/auth-actions.ts</files>
  <action>
    Delete `src/lib/firebase/auth-actions.ts`. This file contains server-side auth actions that were replaced by client-side Firebase SDK calls. It is never imported by any component.

    Verify no imports reference it before deleting:
    ```
    grep -r "auth-actions" src/
    ```
  </action>
  <verify>
    File `src/lib/firebase/auth-actions.ts` no longer exists.
    `npm run build` passes.
  </verify>
  <done>Dead code removed. No unused server action file.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- SetupWizard CTA links to /glasses (not /dashboard/glasses)
- Save-to-history button visible in empty-collection ResultsSheet
- Empty-collection save uses idealGlass as the recommended glass
- /history in middleware's isProtectedRoute check
- auth-actions.ts deleted
- No broken imports
</verification>

<success_criteria>
- UX-02: SetupWizard guides new users to /glasses successfully
- HIST-01: App records beer selections in all scenarios (including empty collection)
- Middleware consistently protects all dashboard routes including /history
- No dead code in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/07-audit-gap-fixes/07-01-SUMMARY.md`
</output>
</content>
</invoke>