---
phase: 04-qr-check-in-flow
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/c/[userId]/page.tsx
autonomous: false

must_haves:
  truths:
    - "Dashboard shows a 'My QR Code' card linking to /qr alongside existing cards"
    - "Logged-in host scanning another host's QR sees a check-in confirmation before viewing glasses"
    - "Host scanning their OWN QR code sees the full drinker experience (identical to a guest)"
  artifacts:
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Dashboard with QR Code card added to the grid"
    - path: "src/app/c/[userId]/page.tsx"
      provides: "Check-in page with logged-in user detection and confirmation dialog"
  key_links:
    - from: "src/app/(dashboard)/dashboard/page.tsx"
      to: "/qr"
      via: "Link href"
      pattern: "href.*['\"/]qr['\"]"
    - from: "src/app/c/[userId]/page.tsx"
      to: "src/components/auth/AuthProvider.tsx"
      via: "useAuth hook for dual-role detection"
      pattern: "useAuth"
---

<objective>
Wire the QR code page into the dashboard navigation and implement dual-role handling for logged-in users visiting check-in pages.

Purpose: Hosts need easy access to their QR code from the dashboard, and logged-in users scanning another host's QR need a smooth dual-role experience with confirmation before checking in.

Output: Updated dashboard with QR card, and check-in page enhanced with logged-in user detection and confirmation dialog.
</objective>

<execution_context>
@/Users/francisdall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/francisdall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-qr-check-in-flow/04-CONTEXT.md
@.planning/phases/04-qr-check-in-flow/04-01-SUMMARY.md
@.planning/phases/04-qr-check-in-flow/04-02-SUMMARY.md
@src/app/(dashboard)/dashboard/page.tsx
@src/app/c/[userId]/page.tsx
@src/components/auth/AuthProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QR Code card to dashboard and dual-role check-in confirmation</name>
  <files>
    src/app/(dashboard)/dashboard/page.tsx
    src/app/c/[userId]/page.tsx
  </files>
  <action>
    1. **Update `src/app/(dashboard)/dashboard/page.tsx`:**
       - Add a third card to the dashboard grid: "My QR Code" card
       - Change grid from `md:grid-cols-2` to `md:grid-cols-3` to accommodate 3 cards
       - New card (Link to /qr):
         - Title: "My QR Code" (same styling as other cards: text-xl, font-semibold)
         - Description: "Generate and share your QR code for guests" (text-sm, text-gray-600)
       - Card order: "Manage My Glasses" | "My QR Code" | "Check In Somewhere"
       - Keep all existing dashboard functionality unchanged (logout, setup wizard, glass guide link)

    2. **Update `src/app/c/[userId]/page.tsx`** to add dual-role handling:
       - Import `useAuth` from `@/components/auth/AuthProvider`
       - Add state: `checkedIn` (boolean, false) — tracks whether logged-in user has confirmed check-in
       - After loading completes and userNotFound is false, check if current user is logged in via `useAuth()`
       - NOTE: `useAuth` may not be available if AuthProvider isn't wrapping the /c/ route. Since `/c/[userId]` is outside the (dashboard) route group, it doesn't have the dashboard layout with AuthProvider. Solution: use `useAuth` with a try/catch or optional pattern. Better: import `auth` from firebase/auth and use `onAuthStateChanged` directly, or import `getAuth` and check `auth.currentUser` in a useEffect.

       REVISED: Use Firebase auth directly (don't depend on AuthProvider context):
       - Import `auth` from `@/lib/firebase/auth`
       - Import `onAuthStateChanged` from `firebase/auth`
       - Add state: `currentUser` (User | null), `authLoading` (boolean, true)
       - In useEffect, subscribe to `onAuthStateChanged(auth, (user) => { setCurrentUser(user); setAuthLoading(false); })`
       - Clean up subscription on unmount

       - If `currentUser` exists AND `currentUser.uid !== userId` (visiting ANOTHER host):
         - Before showing glasses, show a confirmation overlay/card:
           - "Check in at {hostName}'s place?"
           - "You'll see their glass collection and can find the perfect glass for your beer."
           - "Check In" button (amber-600) — sets `checkedIn = true` to dismiss confirmation
           - "Back to Dashboard" link — navigates to /dashboard
         - Only show the glass collection after user clicks "Check In" (checkedIn === true)

       - If `currentUser` exists AND `currentUser.uid === userId` (own QR code):
         - Show the full drinker experience immediately (no confirmation needed)
         - Per context decision: host scanning own QR sees identical experience to any guest

       - If `currentUser` is null (anonymous/unauthenticated):
         - Show glasses immediately (no confirmation needed)
         - This is the standard drinker flow

       - Show the standard header/nav for return to dashboard if logged in (Link to /dashboard at top)
  </action>
  <verify>
    Run `npx tsc --noEmit` to confirm no type errors. Run `npm run build` to verify both pages compile successfully.
  </verify>
  <done>
    Dashboard has 3 cards in a grid. QR Code card links to /qr. Check-in page detects logged-in users and shows confirmation for visiting another host's collection. Own QR code shows full drinker experience without confirmation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete QR check-in flow:
    1. Dashboard with "My QR Code" card
    2. /qr page with QR code display, download, and share
    3. /c/{userId} check-in page with read-only glass collection
    4. Dual-role handling (confirmation for logged-in users at another host's place)
    5. Coming-soon beer teaser on check-in page
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Log in as a host and navigate to the dashboard
    3. Verify 3 cards appear: "Manage My Glasses", "My QR Code", "Check In Somewhere"
    4. Click "My QR Code" — verify /qr page shows a QR code
    5. Click "Download" — verify a PNG file downloads
    6. Copy the check-in URL shown below the QR code
    7. Open that URL in a new incognito/private window (unauthenticated) — verify host's glasses appear in read-only mode
    8. If the host has no glasses, verify the "hasn't added any glasses yet" message appears
    9. Visit /c/invalid-user-id — verify error page appears with signup CTA
    10. Visit your own check-in URL while logged in — verify full drinker experience (no confirmation)
    11. (If you have a second account) Visit the other host's check-in URL while logged in — verify confirmation dialog appears
    12. Verify "Pick a Beer" teaser appears at bottom of check-in page
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Dashboard renders 3 cards in grid layout
2. QR card links to /qr page
3. Logged-in user at another host's /c/ URL sees confirmation
4. Logged-in user at own /c/ URL sees glasses immediately
5. Anonymous user at /c/ URL sees glasses immediately
6. `npm run build` succeeds
</verification>

<success_criteria>
- Dashboard shows all 3 action cards (Manage Glasses, QR Code, Check In)
- Dual-role: logged-in hosts get confirmation dialog at other host's collection
- Own QR code: host sees full drinker experience (per context decision)
- Anonymous users: see glasses immediately without any gate
- Full end-to-end flow works: generate QR → scan → view glasses
</success_criteria>

<output>
After completion, create `.planning/phases/04-qr-check-in-flow/04-03-SUMMARY.md`
</output>
