---
phase: 05-beer-lookup
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/components/beer/BeerInfoSheet.tsx
  - src/components/beer/ManualEntryForm.tsx
  - src/app/c/[userId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "After selecting a beer, page displays beer info in a slide-up bottom sheet"
    - "Beer info sheet shows name, style, ABV, brewery, and description by default"
    - "Beer info sheet has a 'Show details' toggle for IBU and additional info"
    - "Beer info sheet has a 'Match to glasses' CTA button"
    - "When beer is not found, drinker can enter beer details manually"
    - "Manual entry requires style selection and optionally accepts name, ABV, IBU"
    - "Manually entered beer displays in the same info sheet format"
  artifacts:
    - path: "src/components/beer/BeerInfoSheet.tsx"
      provides: "Mobile bottom sheet displaying beer details with progressive disclosure"
      min_lines: 50
    - path: "src/components/beer/ManualEntryForm.tsx"
      provides: "Fallback form for manual beer entry when not found in search"
      min_lines: 40
    - path: "src/app/c/[userId]/page.tsx"
      provides: "Check-in page with complete beer lookup flow including sheet and manual entry"
  key_links:
    - from: "src/components/beer/BeerInfoSheet.tsx"
      to: "react-modal-sheet"
      via: "Sheet component import"
      pattern: "import.*Sheet.*from.*react-modal-sheet"
    - from: "src/components/beer/BeerInfoSheet.tsx"
      to: "src/lib/beer/types.ts"
      via: "Beer type for props"
      pattern: "import.*Beer.*from.*types"
    - from: "src/components/beer/ManualEntryForm.tsx"
      to: "src/lib/beer/styles.ts"
      via: "getAllStyles for style dropdown"
      pattern: "import.*getAllStyles.*from.*styles"
    - from: "src/app/c/[userId]/page.tsx"
      to: "src/components/beer/BeerInfoSheet.tsx"
      via: "renders sheet when beer selected"
      pattern: "BeerInfoSheet.*selectedBeer"
---

<objective>
Create the beer info bottom sheet with progressive disclosure, manual entry fallback form, and wire the complete beer lookup flow on the check-in page.

Purpose: Complete BEER-03 (display beer info) and the not-found fallback path, delivering the full beer discovery experience from search/browse through information display to the "Match to glasses" handoff point for Phase 6.
Output: BeerInfoSheet component, ManualEntryForm component, and fully wired check-in page with complete beer lookup flow.
</objective>

<execution_context>
@/Users/francisdall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/francisdall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-beer-lookup/05-CONTEXT.md
@.planning/phases/05-beer-lookup/05-RESEARCH.md
@.planning/phases/05-beer-lookup/05-02-SUMMARY.md
@src/app/c/[userId]/page.tsx
@src/lib/beer/types.ts
@src/lib/beer/styles.ts
@src/components/beer/BeerSearch.tsx
@src/components/beer/StyleBrowser.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BeerInfoSheet and ManualEntryForm components</name>
  <files>src/components/beer/BeerInfoSheet.tsx, src/components/beer/ManualEntryForm.tsx</files>
  <action>
    **Create `src/components/beer/BeerInfoSheet.tsx`:**

    A 'use client' component using react-modal-sheet for a mobile-friendly slide-up panel.

    Props:
    - `beer: Beer | null`
    - `isOpen: boolean`
    - `onClose: () => void`
    - `onMatchToGlasses: (beer: Beer) => void` (Phase 6 hook - for now just calls the callback)
    - `onChangeBeer: () => void` (reset selection)

    Implementation:
    - Import `Sheet` from `react-modal-sheet`
    - Sheet.Container with Sheet.Header (drag handle) and Sheet.Content
    - Sheet.Backdrop with onTap={onClose}
    - Default view (always visible):
      - Beer image (if `beer.imageUrl` exists, show `<img>` with rounded corners; if null, show a placeholder icon/div with beer mug emoji or amber background)
      - Beer name (text-2xl bold)
      - Brewery name (text-gray-600)
      - Style badge (inline rounded pill with amber-100 bg, amber-800 text)
      - ABV display (e.g., "5.6% ABV") - show "ABV unknown" if null
      - Description text (text-sm text-gray-700)
    - "Show details" toggle (state: `showDetails: boolean`):
      - When expanded: IBU (if available), source indicator (API / Style Browse / Manual Entry)
      - Smooth expand/collapse (simple conditional render is fine, no animation needed)
    - "Match to glasses" button: full-width, amber-600 bg, white text, calls `onMatchToGlasses(beer)`
    - "Choose a different beer" link below button: text-amber-600, calls `onChangeBeer`

    IMPORTANT from RESEARCH.md: react-modal-sheet renders in a portal by default, which solves z-index issues. Do NOT add manual z-index hacks. Ensure no parent elements have `isolation: isolate` or `transform` that would break the stacking context.

    **Create `src/components/beer/ManualEntryForm.tsx`:**

    A 'use client' component for when a beer isn't found in search.

    Props:
    - `onSubmit: (beer: Beer) => void` - called with constructed Beer object
    - `onCancel: () => void` - go back to search/browse

    State: `name`, `style` (required), `abv`, `ibu` - all as strings for form input, parsed on submit

    Implementation:
    - Import `getAllStyles` from `@/lib/beer/styles` for style dropdown options
    - Form fields (all visible, no accordion):
      - Beer Name: text input (optional - default to "Custom Beer" if empty)
      - Style: `<select>` dropdown populated from `getAllStyles()` - REQUIRED, show validation error if empty on submit
      - ABV: number input with step="0.1" (optional)
      - IBU: number input (optional)
    - Submit button: "View Beer Info" (amber-600)
    - Cancel link: "Back to search" (text-amber-600)
    - On submit:
      - Validate style is selected (show inline error if not)
      - Construct Beer object:
        - `id`: `'manual-' + Date.now()`
        - `name`: entered name or "Custom Beer"
        - `style`: selected style name
        - `abv`: parsed float or null
        - `ibu`: parsed int or null
        - `description`: `"Manually entered {styleName}"`
        - `brewery`: "Unknown"
        - `imageUrl`: null
        - `source`: 'manual'
      - Call `onSubmit(beer)`

    Styling: Clean form with labeled inputs, consistent with app's Tailwind patterns. Error state on style field (red border, red text).
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    BeerInfoSheet imports from react-modal-sheet.
    ManualEntryForm imports getAllStyles from styles.ts.
    Both files have 'use client' directive.
  </verify>
  <done>BeerInfoSheet displays beer details in a mobile bottom sheet with progressive disclosure and "Match to glasses" CTA. ManualEntryForm collects beer characteristics with style as required field and constructs a Beer object.</done>
</task>

<task type="auto">
  <name>Task 2: Wire complete beer lookup flow on check-in page</name>
  <files>src/app/c/[userId]/page.tsx</files>
  <action>
    Update check-in page to integrate BeerInfoSheet and ManualEntryForm into the full flow.

    Add imports: BeerInfoSheet, ManualEntryForm

    Add state:
    - `showInfoSheet: boolean` (default false)
    - `showManualEntry: boolean` (default false)

    Update the flow (building on Plan 02's work):

    1. **When user selects a beer (from search or browse):**
       - `handleBeerSelect` now also sets `showInfoSheet = true`
       - BeerInfoSheet opens as bottom sheet overlay

    2. **BeerInfoSheet interactions:**
       - "Match to glasses": For now, show a toast/alert "Glass matching coming in Phase 6!" (simple `alert()` is fine as placeholder). Do NOT navigate anywhere yet.
       - "Choose a different beer": sets `selectedBeer = null`, `showInfoSheet = false`, `showBeerLookup = true`
       - Close/backdrop tap: sets `showInfoSheet = false` (beer stays selected, can reopen)

    3. **Manual entry path:**
       - In the search tab, below the BeerSearch component, add a "Can't find your beer?" link that sets `showManualEntry = true` and `showBeerLookup = false`
       - When `showManualEntry`: render ManualEntryForm
       - ManualEntryForm `onSubmit`: sets `selectedBeer`, `showManualEntry = false`, `showInfoSheet = true`
       - ManualEntryForm `onCancel`: sets `showManualEntry = false`, `showBeerLookup = true`

    4. **Selected beer summary card** (from Plan 02):
       - Add a "View details" button that reopens the info sheet (`showInfoSheet = true`)
       - The existing "Match to glasses" button on the summary card should also open the info sheet

    5. Render the BeerInfoSheet at the bottom of the component (it's a portal, position doesn't matter):
       ```jsx
       <BeerInfoSheet
         beer={selectedBeer}
         isOpen={showInfoSheet}
         onClose={() => setShowInfoSheet(false)}
         onMatchToGlasses={(beer) => alert('Glass matching coming soon!')}
         onChangeBeer={handleChangeBeer}
       />
       ```

    Ensure all state transitions are clean - no way to get stuck in a broken state. The flow is:
    - Choose Beer -> Search/Browse -> Select -> Info Sheet -> (Match or Change)
    - Choose Beer -> Search -> Can't find -> Manual Entry -> Info Sheet -> (Match or Change)
  </action>
  <verify>
    `npm run build` passes.
    Full flow works: Choose Beer -> Search -> Select result -> Bottom sheet opens with beer info.
    Full flow works: Choose Beer -> Browse -> Pick style -> Pick example -> Bottom sheet opens.
    Full flow works: Choose Beer -> Search -> Can't find -> Manual entry -> Submit -> Bottom sheet opens.
    "Match to glasses" shows placeholder alert.
    "Choose a different beer" returns to search/browse.
    No state gets stuck - all paths are recoverable.
  </verify>
  <done>Check-in page has the complete beer lookup flow: search, browse, manual entry, info sheet display, and "Match to glasses" placeholder. All three BEER requirements (01, 02, 03) are satisfied.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Full user journey: Visit /c/{userId} -> see glasses -> Choose a Beer -> Search "IPA" -> select from results -> bottom sheet shows name, style, ABV, brewery, description
- Full user journey: Visit /c/{userId} -> Choose a Beer -> Browse by Style -> Ales -> IPA -> select example -> bottom sheet shows info
- Full user journey: Visit /c/{userId} -> Choose a Beer -> Search -> "Can't find?" -> fill manual form -> submit -> bottom sheet shows entered info
- Bottom sheet has "Show details" toggle, "Match to glasses" button, "Choose a different beer" link
- Manual entry validates that style is required
- No console errors, no broken state transitions
</verification>

<success_criteria>
- BEER-01: Drinker can search by name with results (via Plan 02 search + this plan's sheet display)
- BEER-02: Drinker can browse/select by style (via Plan 02 browser + this plan's sheet display)
- BEER-03: After selecting a beer, page displays name, style, ABV in bottom sheet with progressive disclosure
- Manual entry provides fallback when beer isn't in database
- "Match to glasses" CTA exists and is wired (placeholder action until Phase 6)
- All paths converge to the same BeerInfoSheet display
</success_criteria>

<output>
After completion, create `.planning/phases/05-beer-lookup/05-03-SUMMARY.md`
</output>
