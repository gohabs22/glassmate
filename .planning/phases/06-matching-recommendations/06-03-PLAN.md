---
phase: 06-matching-recommendations
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/lib/firebase/history-db.ts
  - firestore.rules
  - src/app/(dashboard)/history/page.tsx
  - src/app/(dashboard)/dashboard/page.tsx
  - src/app/c/[userId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "saveHistoryEntry writes beer selection to Firestore users/{userId}/history subcollection"
    - "getUserHistory returns history entries ordered by timestamp descending"
    - "Firestore rules allow authenticated users to read/write their own history"
    - "History page at /history shows beer history list with beer name, style, glass, host, and count"
    - "Dashboard links to history page"
    - "'Save to history' button on ResultsSheet calls saveHistoryEntry for logged-in users"
  artifacts:
    - path: "src/lib/firebase/history-db.ts"
      provides: "Firestore CRUD for beer history subcollection"
      exports: ["saveHistoryEntry", "getUserHistory"]
    - path: "src/app/(dashboard)/history/page.tsx"
      provides: "Beer history page showing past selections"
      min_lines: 30
    - path: "src/app/(dashboard)/dashboard/page.tsx"
      provides: "Updated dashboard with history link"
  key_links:
    - from: "src/lib/firebase/history-db.ts"
      to: "src/lib/firebase/config.ts"
      via: "Firestore db import"
      pattern: "import.*db.*from.*config"
    - from: "src/app/c/[userId]/page.tsx"
      to: "src/lib/firebase/history-db.ts"
      via: "saveHistoryEntry call"
      pattern: "import.*saveHistoryEntry.*from.*history"
---

<objective>
Implement beer history storage, display, and wire the save button to Firestore.

Purpose: Complete HIST-01 (record beer selections) and HIST-02 (view history), and fully connect the "Save to history" button in the results sheet to Firestore.
Output: History database operations, /history page, dashboard link, and wired save button.
</objective>

<execution_context>
@/Users/francisdall/.claude/get-shit-done/workflows/execute-plan.md
@/Users/francisdall/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-matching-recommendations/06-CONTEXT.md
@.planning/phases/06-matching-recommendations/06-RESEARCH.md
@.planning/phases/06-matching-recommendations/06-02-SUMMARY.md
@src/lib/firebase/config.ts
@src/app/(dashboard)/dashboard/page.tsx
@src/app/c/[userId]/page.tsx
@src/components/beer/ResultsSheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create history database operations and update Firestore rules</name>
  <files>src/lib/firebase/history-db.ts, firestore.rules</files>
  <action>
    **Create `src/lib/firebase/history-db.ts`:**

    Export type inline (no separate types file needed):
    ```typescript
    export type HistoryEntry = {
      id: string;
      hostUserId: string;
      hostName: string;
      beer: {
        name: string;
        style: string;
        abv: number | null;
        brewery: string;
      };
      recommendedGlass: {
        glassId: string;
        glassName: string;
      };
      timestamp: number;
    };
    ```

    Functions:
    - `saveHistoryEntry(userId, hostUserId, hostName, beer, recommendedGlass)`: Writes to `users/{userId}/history` subcollection using `addDoc`. Store beer as a plain object (name, style, abv, brewery), recommendedGlass as {glassId, glassName}, plus hostUserId, hostName, and `timestamp: Date.now()`.
    - `getUserHistory(userId): Promise<HistoryEntry[]>`: Query `users/{userId}/history` ordered by timestamp desc, return all entries.

    Import `db` from `@/lib/firebase/config`. Use `collection`, `addDoc`, `query`, `orderBy`, `getDocs` from `firebase/firestore`.

    Pattern: Follow existing `src/lib/firebase/public-glasses-db.ts` for Firestore operation style.

    **Update `firestore.rules`:**
    Add rule for history subcollection:
    ```
    match /users/{userId}/history/{historyId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    ```
    Place inside the existing `match /users/{userId}` block if one exists, otherwise add a new match block.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    history-db.ts exports saveHistoryEntry and getUserHistory.
    Firestore rules include history subcollection rule.
  </verify>
  <done>History Firestore operations ready. Authenticated users can read/write their own history subcollection.</done>
</task>

<task type="auto">
  <name>Task 2: Create history page and wire save button</name>
  <files>src/app/(dashboard)/history/page.tsx, src/app/(dashboard)/dashboard/page.tsx, src/app/c/[userId]/page.tsx</files>
  <action>
    **Create `src/app/(dashboard)/history/page.tsx`:**

    A 'use client' page that shows the user's beer history.

    Implementation:
    - Import `getUserHistory` from `@/lib/firebase/history-db`
    - Import auth state (use same `onAuthStateChanged` pattern as other dashboard pages)
    - On mount, fetch history entries for current user
    - Display as a list of cards/rows:
      - Beer name (font-semibold) + style badge (amber pill)
      - Recommended glass name
      - Host name ("at {hostName}'s place")
      - Date (formatted from timestamp: `new Date(timestamp).toLocaleDateString()`)
    - Empty state: "No beer history yet. Check in at a friend's place to get started!"
    - Back link to dashboard
    - Page title: "Beer History"

    Group entries by host and show count per host: "You've had {n} beers at {hostName}'s place". Use client-side grouping (iterate entries, count by hostUserId). Show the count as a subtle label on each entry.

    **Update `src/app/(dashboard)/dashboard/page.tsx`:**
    Add a 4th action card to the dashboard grid: "Beer History" with a brief description and link to /history. Use consistent styling with existing cards (amber-600 accent).

    **Update `src/app/c/[userId]/page.tsx`:**
    Wire the save button in ResultsSheet to actually save to Firestore:

    - Import `saveHistoryEntry` from `@/lib/firebase/history-db`
    - Update the onSaveToHistory handler:
      ```typescript
      async function handleSaveToHistory() {
        if (!currentUser || !selectedBeer || !matchResult?.bestAvailable) return;
        await saveHistoryEntry(
          currentUser.uid,
          userId, // host's userId from URL params
          hostName,
          selectedBeer,
          {
            glassId: matchResult.bestAvailable.glass.id,
            glassName: matchResult.bestAvailable.glass.name,
          }
        );
        setHistorySaved(true);
      }
      ```
    - Pass this handler to ResultsSheet's onSaveToHistory prop (replacing the placeholder)
  </action>
  <verify>
    `npm run build` passes.
    /history page renders for logged-in users.
    Dashboard has "Beer History" card linking to /history.
    "Save to history" button on ResultsSheet calls saveHistoryEntry.
    History entries display with beer name, style, glass, host name, and count.
  </verify>
  <done>Beer history is fully functional: save from results sheet, view on /history page, accessible from dashboard. Each entry shows beer, glass, host, and count of beers at that host's place.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- /history page accessible from dashboard
- "Save to history" button triggers Firestore write
- History entries show beer name, style, recommended glass, host name, count
- Firestore rules protect history — users can only read/write their own
- Anonymous users see signup nudge instead of save button
</verification>

<success_criteria>
- HIST-01: App records beer selections when user taps "Save to history"
- HIST-02: Logged-in user can view beer history on /history page
- History shows beer name, style, recommended glass, host name, and beer count per host
- Save is opt-in (not auto-recorded)
- Anonymous users see gentle nudge to sign up
- Dashboard links to history page
</success_criteria>

<output>
After completion, create `.planning/phases/06-matching-recommendations/06-03-SUMMARY.md`
</output>
